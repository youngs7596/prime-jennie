# Session Handoff - 2026-03-11-13:40

## 작업 요약 (What was done)

### Dashboard System 페이지 LLM 사용량 탭 추가
- **기존 LLM 카드 삭제**: Services 탭의 "LLM Usage (Today)" 카드 제거
- **새 LLM 탭 추가**: System 페이지에 Brain 아이콘의 독립 탭
  - 상단: 당일/월간 요약 카드 (호출수, 입력 토큰, 출력 토큰)
  - 하단: 기능별 통합 테이블 (기능명, Provider, 모델, 실행주기, 당일/월간 호출·토큰)
- **5개 기능 매핑**: 뉴스 감성 분석(vLLM), Scout 종목 분석(DeepSeek), Macro Council(DeepSeek+Claude), 데일리 브리핑(Claude), WSJ 뉴스 요약(Claude)

### Backend 변경
- `metrics.py`: Redis TTL 7일→35일 확장, `get_llm_monthly_stats()` 추가 (pipeline 일괄 조회)
- `llm_stats.py`: `/api/llm/features`, `/api/llm/stats/monthly/{YYYY-MM}`, `/api/llm/stats/monthly` 엔드포인트 추가
- `wsj_summary` 서비스 추적 추가 (기존 누락)

### Frontend 변경
- `api.ts`: `LLMFeature`, `LLMMonthlyStats` 타입 + `useLLMFeatures()`, `useLLMMonthlyStats()` 훅
- `System.tsx`: LLM 탭 컴포넌트, `formatTokens()` (K/M 단위 변환)

### 변경된 파일
- `prime_jennie/infra/observability/metrics.py`
- `prime_jennie/infra/observability/__init__.py`
- `prime_jennie/services/dashboard/routers/llm_stats.py`
- `frontend/src/lib/api.ts`
- `frontend/src/pages/System.tsx`
- `tests/unit/services/test_dashboard.py` (+3건: features, monthly_stats, monthly_no_data)

## 현재 상태 (Current State)
- **배포 완료**: CI pass → GHCR 빌드 → 서버 deploy 성공
- **stop=1 유지**: 시그널 감지 + 로그만
- **테스트**: 880 passed, 0 failed

## 다음 할 일 (Next Steps)
- 이 세션에서 새로 발견된 이슈 없음
- 기존 과제 추적: `.ai/TODO.md` 참조

## Context for Next Session
다음 세션 시작 시 아래 파일들을 먼저 읽어주세요:
- `.ai/TODO.md` — 미해결 과제 현황
- `prime_jennie/services/dashboard/routers/llm_stats.py` — LLM 통계 API 구조 확인 시

## 핵심 결정사항 (Key Decisions)
- **Redis TTL 35일**: 월별 집계를 위해 최소 31일 보장 필요. DB 테이블 추가 대신 TTL 확장으로 단순하게 해결
- **Pipeline 일괄 조회**: 월별 집계 시 최대 31일×6서비스=186개 Redis 호출을 pipeline으로 1회 왕복으로 최적화
- **기능 매핑 정적 정의**: config에서 provider/model 동적 조회하되, 기능↔서비스 매핑은 `_FEATURE_MAP`에 하드코딩 (변경 빈도 낮음)
- **Overview 페이지 LLM 카드 유지**: System 탭과 독립적으로 Overview의 간략 카드는 그대로 유지

## 주의사항 (Warnings)
- `/stats/monthly` 라우트가 `/stats/{target_date}` 앞에 정의되어야 함 (path param 충돌 방지)
- TTL 변경은 신규 기록부터 적용됨. 기존 7일 TTL 키는 만료 후 자연 소멸
