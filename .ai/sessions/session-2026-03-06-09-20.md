# Session Handoff - 2026-03-06-09:20

## 작업 요약 (What was done)

### 1. 서버(MS-01) 상태 점검
- CPU(i9-12900H 20코어, load 0.9) 양호, SSD(8%) 여유
- **메모리가 병목**: 31GB 중 vLLM 4.3GB + Docker 22개 + Claude CLI 동시 부하 시 멈춤 발생
- GPU VRAM 85% 사용 (vLLM 2개: EXAONE + KURE)
- OOM 기록 없음 — 커널 페이지 회수 시 시스템 정지 현상으로 추정

### 2. zram 설정 (시스템 멈춤 방지)
- `zram-tools` 설치, `/etc/default/zramswap` 설정 (ALGO=zstd, PERCENT=25)
- 결과: zram 7.8GB (priority 100) + 디스크 swap 8GB (priority -2)
- 메모리 피크 시 디스크 I/O 없이 RAM 내 압축 → 멈춤 방지

### 3. KIS Gateway WebSocket → REST Polling 전환
- 장외 시간 WebSocket 연결 실패 → backoff 600초 대기 → 장 시작 후에도 데이터 없음
- `docker-compose.yml`에 `KIS_STREAMER_MODE`, `KIS_POLLING_INTERVAL_SEC` 환경변수 매핑 추가
- `.env`에 `KIS_STREAMER_MODE=polling`, `KIS_POLLING_INTERVAL_SEC=3` 설정
- gateway recreate → polling 모드 정상 작동 확인 (28종목 구독, 3초 간격)

### 4. Macro Council — Claude Opus 4.6 Chief Judge 전환
- 기존: 3단계 모두 DeepSeek → Chief Judge가 리스크 무시하고 bullish 편향
- 변경: Step 1,2 = DeepSeek (REASONING), **Step 3 = Claude Opus 4.6 (THINKING)**
- `docker-compose.yml` job-worker에 `LLM_TIER_THINKING_PROVIDER`, `LLM_CLAUDE_MODEL`, `ANTHROPIC_API_KEY` 추가
- **Claude JSON 파싱 강화** (`claude.py`): 스키마 힌트 주입, markdown/non-JSON fallback 추출
- **필드명 정규화** (`pipeline.py`): `_normalize_chief_judge()` — LLM마다 다른 키명 흡수
- 결과: neutral(53), 포지션 75%, 손절 90%, 선호 조선/방산/금융, 회피 화학/에너지

### 5. 전반적인 시스템 점검
- 22개 Docker 컨테이너 전부 healthy
- Redis 스트림 정상 (buy-signals 0, news:raw 11K, kis:prices 10K)
- news analyzer lag 109건, archiver lag 172건 (양호)
- trading_flags:stop=1, pause=emergency_stop — 매매 정지 중
- Macro council 정상 스케줄링 (07:50, 11:50 KST)
- Minute chart 수집 500 에러 — gateway recreate 시점 일시적 발생 (이후 정상)

## 현재 상태 (Current State)

- **KIS Gateway**: REST polling 모드 (3초), 28종목 구독 중
- **Macro Council**: Claude Opus 4.6 Chief Judge, 직전 실행 성공
- **Trading**: stop + emergency_stop — 매매 정지 중
- **zram**: 7.8GB 활성, 재부팅 후에도 자동 적용
- **미배포**: `collector.py` NEWS_STREAM_MAXLEN 변경 (이전 세션에서 코드 수정, 이번 커밋에 미포함)

## 다음 할 일 (Next Steps)

- [ ] (긴급) WebSocket streamer backoff 로직 개선 — 장 시작 시 backoff 리셋 필요
  - 현재: 장외 실패 시 backoff가 600초까지 증가 → 장 시작 후에도 긴 대기
  - 개선: `_is_streaming_hours()` 진입 시 backoff를 `_BACKOFF_INITIAL`로 리셋
- [ ] (중요) WebSocket vs Polling 자동 전환 검토 — 장중 WebSocket 연결 가능 여부 재테스트
- [ ] (중요) /stop, /pause 해제 전 buy-signals 밀린 시그널 확인
- [ ] (중요) News Pipeline 구조 개선 — `.ai/TODO.md` #10
- [ ] (중요) `collector.py` MAXLEN 변경 배포 (다음 push에 포함 필요)
- [ ] (낮음) Docker 미사용 이미지 정리 (vllm v0.15.1, v0.16.0-cu130 ~56GB)
- [ ] (낮음) Telegram collector EOFError 수정 (`@hedgecat0301` 세션 만료)

## Context for Next Session

다음 세션 시작 시 아래 파일들을 먼저 읽어주세요:
- `prime_jennie/services/gateway/streamer.py` — WebSocket backoff 개선 대상 (line 212-259)
- `prime_jennie/services/council/pipeline.py` — Council 3단계 파이프라인 + 정규화 로직
- `prime_jennie/infra/llm/providers/claude.py` — Claude JSON 파싱 로직
- `.ai/TODO.md` — 미해결 과제 목록

## 핵심 결정사항 (Key Decisions)

- **zram 도입**: swap 늘리기보다 zram(RAM 내 압축)이 멈춤 방지에 효과적
- **Claude Opus 4.6 for Chief Judge only**: 전체 Claude 시 $2.5~4/회, Chief만 $0.5~1/회 — 가성비 최적
- **DeepSeek 유지 (Step 1,2)**: 데이터 수집/분석은 DeepSeek으로 충분, 최종 판정만 고급 모델
- **REST Polling 전환**: WebSocket 장외 불안정 → polling이 안정적 (3초 간격, 종목별 REST)
- **필드명 정규화**: LLM마다 JSON 키가 다를 수 있으므로 대체 키 매핑으로 방어적 처리

## 주의사항 (Warnings)

- **장중 push 금지** — Gateway 재시작 위험 (단, trading stop 상태면 영향 적음)
- **장중 kis-gateway 재시작 금지** — 토큰 캐시 소실 → KIS 403 rate limit
- **.env 변경은 docker-compose recreate 필요** — restart로는 환경변수 미반영
- **GHCR 이미지 사용** — 로컬 코드 변경 시 반드시 `docker compose build` 후 recreate
- **NEWS_STREAM_MAXLEN 코드 변경 미배포** — collector.py 수정이 아직 이전 커밋에만 있음
