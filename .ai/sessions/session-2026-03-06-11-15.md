# Session Handoff - 2026-03-06-11:15

## 작업 요약 (What was done)

### 1. WebSocket backoff 리셋 로직 개선
- `streamer.py`: 장외 시간 대기 중 `backoff = _BACKOFF_INITIAL` 리셋 추가
- 효과: 장 시작(08:50) 시 즉시 연결 시도 (기존: backoff 600초까지 누적)

### 2. Stop 상태 시그널 억제 + signal_logs 백테스트 이력
- **Scanner** `_publish_signal()`: stop/pause 시 Stream 발행 차단, DB 이력만 저장
- **Monitor** `_emit_sell_order()`: 동일하게 차단 + DB 이력 저장
- **signal_logger.py**: 매수/매도 공통 DB 기록 헬퍼 신규 생성
- **Alembic 010**: `signal_logs` 테이블 (BUY/SELL, suppressed/published 구분)
- MS-01에서 `alembic upgrade head` 실행 완료

### 3. 서버 운영 정리
- **News pipeline**: analyzer/archiver consumer group 위치 `$`로 리셋 (lag 0)
- **kis:prices 레거시 그룹 삭제**: `price-monitor-group`(pending 475K), `buy-scanner-group` 제거
- **Docker 미사용 이미지 정리**: vLLM v0.15.1(29.5GB) + v0.16.0(29.8GB) 삭제 (~59GB 회수)
- **collector.py MAXLEN**: 이미 10,000으로 반영 확인

### 4. 시스템 전체 점검
- 22개 컨테이너 healthy, Redis lag 전부 0, 디스크 4% 사용

## 현재 상태 (Current State)

- **KIS Gateway**: REST polling 모드 (3초 간격, `.env` KIS_STREAMER_MODE=polling)
- **Trading**: stop=1, pause=emergency_stop — 매매 정지 중
- **signal_logs**: 테이블 생성 완료, 장 시작 후 시그널 발생 시 기록 시작
- **Scanner**: min_bars 부족 (배포 재시작 후 바 재축적 중, 장 시작 후 해소)
- **WebSocket backoff 리셋**: 코드 배포 완료, 다음 WebSocket 모드 전환 시 효과 확인 가능

## 다음 할 일 (Next Steps)

- [ ] (중요) **News Pipeline 구조 개선** — TODO #10
  - collector → analyzer → archive 순차 구조가 근본 원인
  - 해결안 후보: (A) collector/analyzer 병렬 스레드, (B) watchlist 종목만 수집, (C) analyzer 별도 서비스
  - analyzer LLM 호출 실패 시 재시도/dead-letter 메커니즘 필요
- [ ] (중요) WebSocket vs Polling 자동 전환 검토 — 장중 WebSocket 연결 안정성 테스트
- [ ] (중요) /stop, /pause 해제 시점 결정 — 매매 재개 준비
- [ ] (낮음) Telegram collector EOFError 수정 (`@hedgecat0301` 세션 만료)

## Context for Next Session

다음 세션 시작 시 아래 파일들을 먼저 읽어주세요:
- `prime_jennie/services/news/app.py` — News pipeline 메인 루프 (순차 구조)
- `prime_jennie/services/news/analyzer.py` — Analyzer consumer (LLM 호출)
- `prime_jennie/services/news/collector.py` — Collector (네이버 크롤링)
- `prime_jennie/services/news/archiver.py` — Archiver (Qdrant 임베딩)
- `.ai/TODO.md` #10 — News Pipeline 개선 분석

## 핵심 결정사항 (Key Decisions)

- **시그널 억제 위치**: Executor가 아닌 Scanner/Monitor(발행 측)에서 차단 — Stream에 불필요한 메시지 안 쌓임
- **published 시그널도 기록**: suppressed뿐 아니라 실제 발행 시그널도 signal_logs에 저장 → 백테스트 비교 가능
- **레거시 consumer group 삭제**: `buy-scanner-group`, `price-monitor-group`은 코드에서 미사용 확인 후 삭제
- **News lag은 리셋으로 임시 해소**: 구조적 해결은 다음 세션 News Pipeline 개선에서 진행

## 주의사항 (Warnings)

- **KIS Gateway 현재 polling 모드** — WebSocket 전환 시 `.env` KIS_STREAMER_MODE=websocket + gateway recreate 필요
- **장중 push 금지** — 09:00~15:30 KST
- **장중 kis-gateway 재시작 절대 금지** — 토큰 캐시 소실 위험
- **signal_logs FK**: stock_masters.stock_code 참조 — 시딩 안 된 종목 시그널은 FK 에러 가능 (warning 로그만 남김)
