# Session Handoff - 2026-03-05-23:50

## 작업 요약 (What was done)

### 1. KIS REST Polling Fallback 구현
- WebSocket 차단 시 REST API 폴링으로 전환하는 `KISRestPoller` 구현
- 3초 간격, 장외시간 60초 sleep, 종목별 API 실패 시 skip (전체 중단 아님)
- `KIS_STREAMER_MODE` 환경변수로 websocket/polling 전환
- `KIS_POLLING_INTERVAL_SEC`으로 폴링 간격 조정
- **변경 파일**:
  - `prime_jennie/services/gateway/poller.py` (신규) — KISRestPoller 클래스
  - `prime_jennie/services/gateway/app.py` — lifespan에서 streamer_mode 분기
  - `prime_jennie/domain/config.py` — KISConfig에 streamer_mode, polling_interval_sec 추가
  - `tests/unit/services/test_poller.py` (신규) — 18개 테스트
  - `tests/unit/services/test_streamer.py` (신규) — streamer 개선 테스트

### 2. Streamer 개선 (이전 세션 변경 포함)
- hot subscribe (재시작 없이 WS에서 구독 추가)
- remove_subscriptions 메서드 추가
- exponential backoff (안정 연결 30초 이상 시 리셋)
- 장외 시간 대기 (08:50~15:35 KST 외 60초 sleep)
- thread safety (lock 추가)

### 3. 배포
- CI 통과 (lint + unit + e2e), GHCR Publish + Deploy 완료
- 커밋: `e3f03e3` feat, `cf38a06` style fix

### 4. Redis 스트림 정리
- `stream:buy-signals` 904건, `stream:sell-orders` 535건, `stream:trade-notifications` 50건 → 전부 XTRIM 0
- `stream:news:raw` 100,006건 → XTRIM 10,000건 (28MB → ~3MB)
- news consumer group (analyzer, archiver) last-delivered-id → `$` 리셋 (lag 0)

### 5. News Pipeline 문제 발견 및 TODO 등록
- analyzer lag 10,162건 (3/3부터), archiver lag (2/27부터)
- 원인: collect → analyze → archive 순차 실행, collector가 전체 유니버스 크롤 시 수십 분 소요
- `NEWS_STREAM_MAXLEN` 100,000 → 10,000 코드 수정 완료 (미배포)

## 현재 상태 (Current State)

- **배포 완료**: poller + streamer 개선 GHCR 배포됨
- **미배포 변경**: `collector.py` NEWS_STREAM_MAXLEN 변경, `.ai/TODO.md` 업데이트
- **kis-gateway**: 현재 실행 중 아님 (profile: real). 기본 streamer_mode=websocket
- **trading_flags:stop**: 활성 상태 — executor 정지 중
- **내일 오전 테스트 필요**: `KIS_STREAMER_MODE=polling` 설정 후 kis-gateway 기동, 실시간 폴링 확인

## 다음 할 일 (Next Steps)

- [ ] (긴급) 내일 오전 REST Poller 기능 테스트
  - `KIS_STREAMER_MODE=polling` 환경변수 설정
  - `POST /api/realtime/subscribe` → 종목 구독
  - `GET /api/realtime/status` → mode: "polling" 확인
  - Redis `kis:prices` 스트림 데이터 유입 확인
- [ ] (긴급) News Pipeline 구조 개선 — `.ai/TODO.md` #10 참조
- [ ] (중요) `collector.py` MAXLEN 변경 배포 (다음 push 시 포함)
- [ ] (중요) /stop 해제 전 scanner/monitor 시그널 발생 상태 재확인

## Context for Next Session

다음 세션 시작 시 아래 파일들을 먼저 읽어주세요:
- `prime_jennie/services/gateway/poller.py` — REST Poller 구현체
- `prime_jennie/services/gateway/app.py` — lifespan streamer_mode 분기 로직
- `prime_jennie/services/news/app.py` — news pipeline 순차 실행 구조 (개선 대상)
- `.ai/TODO.md` — 미해결 과제 목록 (#10 뉴스 파이프라인)

## 핵심 결정사항 (Key Decisions)

- **duck typing**: Protocol/ABC 없이 KISRestPoller와 KISWebSocketStreamer 동일 인터페이스
- **start(base_url)**: poller는 base_url 불필요하지만, 인터페이스 호환을 위해 파라미터 수용 (무시)
- **get_status()에 mode 필드 추가**: poller는 `"mode": "polling"` 반환하여 구분 가능

## 주의사항 (Warnings)

- **장중 push 금지** — Gateway WebSocket 끊김 위험
- **/stop 해제 전 반드시 buy-signals 스트림 확인** — 밀린 시그널 폭주 방지
- **NEWS_STREAM_MAXLEN 코드 변경 미배포** — 다음 push에 포함 필요
