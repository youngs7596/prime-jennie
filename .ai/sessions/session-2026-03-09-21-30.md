# Session Handoff - 2026-03-09-21:30

## 작업 요약 (What was done)

### 1. RSI_REBOUND 전략 비활성화
- 실전 거래 데이터 확인: 승률 30%, 평균 PnL -1.16%
- signal_logs 기반 가상 수익률 분석: 억제된 RSI_REBOUND 시그널 추적 → -4.7% 평균
- 백테스트 파라미터 스윕 (`scripts/sweep_rsi_threshold.py`): 비활성화가 최적 Sharpe
- 비활성화 적용: scanner, backtest, council prompt, VALID_STRATEGIES

### 2. 데일리 브리핑 중복 발송 수정
- **원인**: Airflow `execution_timeout=5min` + `retries=2` → LLM 느려서 timeout → 재시도 시 중복 발송
- **수정**: Redis `briefing:sent:{date}` 키로 멱등성 체크 (2일 TTL)
- `DailyReporter`에 Redis 주입, `_already_sent_today()` / `_mark_sent_today()` 추가

### 3. LLM 프로바이더 정비
- **문제 발견**: 브리핑/WSJ 요약이 EXAONE(vLLM)을 사용, Council Chief Judge도 DeepSeek
- **수정**:
  - 데일리 브리핑: `fast`(EXAONE) → `ClaudeLLMProvider` (Claude Opus 4.6)
  - WSJ 한글 요약: `fast`(EXAONE) → `ClaudeLLMProvider` (Claude Opus 4.6)
  - Council Chief Judge: `thinking`(DeepSeek) → `ClaudeLLMProvider` (Claude Opus 4.6)
- **EXAONE은 뉴스 감성 분석 전용**으로 한정

### 4. WSJ 뉴스레터 분석
- 월요일(3/9)에 The 10-Point 1개만 수신은 정상 (주말에는 10-Point만 발행)
- Redis 확인: 금요일(3/7)에는 10-point, markets-am, markets-pm 3개 전송

### 5. 텔레그램 스크린샷 분석
- 3장 분석: fallback HTML(17:05) + LLM narrative(17:15) = Airflow 재시도로 2회 발송 확인

### 변경 파일 요약
| 파일 | 변경 내용 |
|------|----------|
| `prime_jennie/services/scanner/strategies.py` | RSI_REBOUND 호출 주석 처리 |
| `prime_jennie/services/backtest/daily_strategies.py` | RSI_REBOUND 호출 주석 처리 |
| `prime_jennie/services/council/pipeline.py` | VALID_STRATEGIES에서 RSI_REBOUND 제거, Chief Judge → Claude |
| `prompts/council/macro_chief_judge.txt` | RSI_REBOUND 제거 (5개 전략) |
| `prime_jennie/services/briefing/reporter.py` | Redis 멱등성 + Claude Opus 전환 |
| `prime_jennie/services/jobs/app.py` | Reporter에 Redis 주입 + WSJ 요약 Claude 전환 |
| `scripts/sweep_rsi_threshold.py` | RSI threshold 파라미터 스윕 스크립트 (신규) |
| `tests/unit/services/test_briefing.py` | 멱등성 테스트 6개 + mock 대상 수정 |
| `tests/unit/services/test_council.py` | judge_provider 파라미터 반영 |

## 현재 상태 (Current State)
- 모든 변경사항 커밋/푸시 완료 (development + main)
- CI 통과, GHCR 이미지 빌드 완료
- 서버 job-worker 재배포 완료 (수동 pull + restart)
- `trading_flags:stop=1` 유지 중

## 다음 할 일 (Next Steps)
- 기존 TODO 항목 중심으로 진행 (TODO.md 참조)
- TODO #22 추가: GHCR deploy CI 타이밍 레이스 — development deploy가 CI 완료 전에 체크하여 실패
- TODO #23 추가: daily_briefing_report execution_timeout 조정 — 현재 5분, LLM 응답 시간 고려 10분으로 확대 검토

## Context for Next Session
다음 세션 시작 시 아래 파일들을 먼저 읽어주세요:
- `.ai/TODO.md` — 미해결 과제 현황
- `prime_jennie/services/council/pipeline.py` — LLM 프로바이더 변경 확인
- `prime_jennie/services/briefing/reporter.py` — 멱등성 로직 확인

## 핵심 결정사항 (Key Decisions)
- **RSI_REBOUND 비활성화**: 3가지 독립 분석(실거래/가상수익률/백테스트) 모두 성과 불량 확인
- **EXAONE은 뉴스 감성 전용**: 빠른 대량 처리에 적합, 고품질 생성(브리핑/요약/판정)은 Claude
- **Council Chief Judge를 Claude로**: DeepSeek thinking → Claude Opus — 최종 의사결정의 품질 향상
- **멱등성은 Redis 기반**: 단순 retries=0보다 견고, 수동 재실행 시에도 보호

## 주의사항 (Warnings)
- DONE.md의 "Council Claude Opus Chief Judge 전환 — ✅ 2026-03-06" 항목은 실제로는 미완성이었음 (코드가 여전히 DeepSeek thinking 사용). 이번 세션에서 실제 수정 완료
- `LLMFactory.get_provider()` 캐시(`@lru_cache`)로 인해 런타임 중 provider 변경 불가 — 서비스 재시작 필요
- 프로젝트 루트에 스크린샷 3장 (Screenshot_*.jpg) 남아있음 — 정리 필요
