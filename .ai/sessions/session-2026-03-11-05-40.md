# Session Handoff - 2026-03-11-05:40

## 작업 요약 (What was done)

### 1. 브리핑 DAG 안정화 (TODO #23 완료)
- `daily_briefing_report` DAG: timeout 5→10분, retries 2→1
- `/report` 엔드포인트: 발송 실패 시 HTTP 500 반환 + 로깅 강화
- Claude Opus 사용으로 인한 timeout 대응 + Airflow 재시도 방지 (멱등성 보호)

### 2. score=50 버그 기간 재분석 완료 (TODO #16 완료)
- 원인 분석: 구시스템(my-prime-jennie) reason 미저장 + model=None 버그 기간(02-20~03-02)만 실제 문제
- vLLM throughput PoC 실행: 동시성 20이 최적 (~4.3 req/s, RTX 3090)
- 배치 재분석 스크립트로 35,905/35,921건 처리, 평균 50.0→63.0점 복원
- `scripts/poc_vllm_throughput.py`, `scripts/reanalyze_sentiment.py`, `scripts/reanalyze_standalone.py` 추가

### 3. 뉴스 감성 분석 배치 처리 (신규)
- Analyzer stream 읽기 `count=1` → `count=20` 배치 처리
- `asyncio.gather`로 LLM 동시 호출 (~0.5 → ~4.3 req/s)
- GPU burst→idle 패턴 전환: 277W/65°C/74%팬 → idle 시 128W/53°C/58%팬
- 테스트 2건 추가 (전체 874 passed)

### 변경 파일
- `dags/utility_jobs_dag.py` — timeout/retries 조정
- `prime_jennie/services/jobs/app.py` — /report 엔드포인트 HTTP 500 반환
- `prime_jennie/services/briefing/reporter.py` — 로깅 강화
- `prime_jennie/services/news/analyzer.py` — 배치 처리 전면 리팩토링
- `tests/unit/services/test_news_pipeline.py` — 배치 테스트 추가
- `scripts/poc_vllm_throughput.py` — vLLM throughput PoC (신규)
- `scripts/reanalyze_sentiment.py` — 배치 재분석 스크립트 (신규)
- `scripts/reanalyze_standalone.py` — standalone 재분석 (신규)
- `.ai/TODO.md` — #16, #23 완료 처리

## 현재 상태 (Current State)
- 전 서비스 정상 운영 중 (development 브랜치, GHCR 배포 완료)
- `trading_flags:stop=1` 유지 중
- GPU idle 시 128W/53°C — 배치 처리 효과 확인됨
- news-pipeline: 배치 단위(최대 20건) 동시 분석 정상 동작

## 다음 할 일 (Next Steps)
- 이 세션에서 새로 발견된 이슈 없음
- 기존 TODO 항목 참조: `.ai/TODO.md`

## Context for Next Session
다음 세션 시작 시 아래 파일들을 먼저 읽어주세요:
- `.ai/TODO.md` — 미해결 과제 현황
- `prime_jennie/services/news/analyzer.py` — 배치 처리 구조 (최근 변경)

## 핵심 결정사항 (Key Decisions)
- **score=50 레거시 42만건 중 버그 기간 3.3만건만 재분석**: 구시스템 데이터는 reason 미저장이 정상이므로 삭제/재분석 불필요
- **뉴스 분석 배치 처리**: GPU 전력/소음 문제 해결을 위해 순차→배치 전환. vLLM continuous batching 활용
- **BATCH_SIZE=20**: PoC에서 동시성 20이 throughput 최적점으로 확인

## 주의사항 (Warnings)
- GPU 전력 제한 275W로 설정됨 (Default 370W의 ~75%). 더 낮춰도 팬 소음 변화 미미 (eGPU 특성)
- GPU 전력 제한은 sudo 필요 (`sudo nvidia-smi -pl N`)
- TODO #16 잔여 16건(35,921 중 16건)은 무시해도 무방
