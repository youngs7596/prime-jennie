# Session Handoff - 2026-03-01-10-41

## 작업 요약 (What was done)

### 1. 강제 청산(Forced Liquidation) 기능 — 2단계 안전장치

긴급 상황 시 특정 종목을 즉시 전량 매도할 수 있는 강제 청산 기능 구현.

**설계: Arm-then-Fire 2단계 안전장치**
- 1단계: Redis SET(`forced_liquidation:stocks`)에 대상 종목 등록
- 2단계: ARM 스위치(`forced_liquidation:armed`) ON → 다음 틱에서 전량 매도
- SET이 비면 ARM 자동 해제

**변경 파일 (6개, +276줄)**
- `prime_jennie/domain/enums.py` — `SellReason.FORCED_LIQUIDATION` 추가
- `prime_jennie/services/monitor/app.py` — `_check_forced_liquidation()` + `_emit_forced_liquidation()` (exit rules 평가보다 선행)
- `prime_jennie/services/seller/executor.py` — `FORCED_LIQUIDATION`을 `MANUAL` 동급 취급 (emergency stop, 장시간 체크 우회)
- `prime_jennie/services/telegram/handler.py` — `/liquidate` 명령어 (add/remove/list/arm/disarm/clear/status)
- `tests/unit/services/test_monitor_app.py` — 4개 테스트 (ARM+대상→매도, ARM+비대상→정상, ARM OFF→스킵, SET 비면 자동해제)
- `tests/unit/services/test_sell_executor.py` — 2개 테스트 (emergency stop 우회, 장시간 우회)

**커밋**: `ba952fb` feat: 강제 청산(Forced Liquidation) 기능 — 2단계 안전장치

### 2. Airflow Webserver healthcheck 시간 연장

- `docker-compose.yml`의 airflow-webserver healthcheck `start_period` 60s → 120s 변경
- Airflow 초기 기동이 느릴 때 healthcheck 실패로 컨테이너 재시작되는 문제 방지

**커밋**: `0163c91` fix: airflow-webserver healthcheck start_period 60s→120s

## 현재 상태 (Current State)
- development 브랜치, 커밋 `0163c91`, push 완료
- 강제 청산 기능: 코드 배포 완료, 텔레그램 `/liquidate` 명령어 사용 가능

## 다음 할 일 (Next Steps)
- [ ] 강제 청산 기능 실전 사용 시 정상 작동 확인
- [ ] 강제 청산 후 trade_log에 FORCED_LIQUIDATION 기록 정상 확인

## Context for Next Session
다음 세션 시작 시 아래 파일들을 먼저 읽어주세요:
- `prime_jennie/services/monitor/app.py:150-366` — 강제 청산 로직 (process_tick 내 위치 확인)
- `prime_jennie/services/telegram/handler.py:607-715` — /liquidate 명령어 핸들러

## 핵심 결정사항 (Key Decisions)
- **2단계 안전장치 채택**: 실수로 전량 매도 방지 위해 add → arm 분리. 한 번에 바로 매도하는 1단계 방식 대신 선택
- **MANUAL 동급 취급**: FORCED_LIQUIDATION은 emergency stop, 장시간 체크, cooldown 모두 우회. 긴급 상황에서 어떤 세이프가드도 매도를 막지 않도록 설계
- **ARM 자동 해제**: SET이 비면 armed 키 자동 삭제 — 청산 완료 후 수동 해제 불필요

## 주의사항 (Warnings)
- ARM이 ON인 상태에서 실수로 다른 종목을 add하면 그 종목도 다음 틱에 매도됨 — 반드시 순서 주의 (add 먼저 → arm 나중에)
- Redis 재시작 시 forced_liquidation SET이 소실될 수 있음 (RDB/AOF 설정에 따라 다름)
- 세션 핸드오프가 누락되어 있어 3/2 resume 시 이 기능이 누락되었음 (Co-Authored-By: Claude Opus 4.6)
