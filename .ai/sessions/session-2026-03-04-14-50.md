# Session Handoff - 2026-03-04-14-50

## 작업 요약 (What was done)

### NVIDIA Driver 590 → 575 마이그레이션 후 서비스 복구

1. **CUDA compat libcuda.so 충돌 해결**
   - `/var/run/cdi/nvidia.yaml`에 이전 드라이버 590 참조 잔존 → CDI 재생성
   - 컨테이너 내부 CUDA compat libcuda.so(580.82.07)가 호스트(580.126.09)보다 먼저 로드 → Error 803
   - `LD_LIBRARY_PATH` 순서 조정으로 호스트 libcuda.so 우선 로드

2. **vLLM v0.16.0-cu130 기동 확인**
   - EXAONE-4.0-32B-AWQ (LLM, port 8001) + KURE-v1 (Embedding, port 8002)
   - 추론/임베딩 테스트 정상 확인
   - `docker-compose.yml`에 이미지 버전 + LD_LIBRARY_PATH 고정
   - HF_HUB_OFFLINE: "1" 복원

3. **Infra 서비스 문제 해결**
   - grafana/loki: `/docker_data` 볼륨 퍼미션 (root → 472/1000)
   - mariadb: healthcheck 유저 비밀번호 재설정

4. **보안 강화**
   - UFW 방화벽 활성화 (deny incoming, allow 22/tcp + LAN 192.168.31.0/24)
   - SSH: PasswordAuthentication no, PermitRootLogin no, MaxAuthTries 3
   - Redis: requirepass 설정 + 전체 앱 서비스 재시작

5. **Monitor 포지션 유실 버그 수정** (`prime_jennie/services/monitor/app.py`)
   - 매도 시그널 발행 시 `_positions.pop()` → live snapshot에서 포지션 사라짐
   - `_pending_sell` set 도입: 시그널 발행 후 즉시 pop 안 하고, 다음 refresh에서 KIS 잔고 확인 후 자연 제거
   - Dashboard portfolio 화면에서 1개만 보이던 문제 해결 → 5개 모두 표시

6. **DB ↔ KIS 포지션 동기화**
   - 대한항공/한국전력 (DB에만 있음) → 삭제
   - 현대차/고려아연/KODEX200/HD현대/대한조선 → 수량/평단가 업데이트

7. **GitHub Actions self-hosted runner 등록**
   - `ms-01` runner: online, systemd 서비스로 자동 시작
   - 이전 offline runner(`prime-jennie-runner`) 제거

### 변경된 파일
- `docker-compose.yml` — vLLM v0.16.0-cu130 고정, LD_LIBRARY_PATH, Redis requirepass
- `prime_jennie/services/monitor/app.py` — _pending_sell 도입, 즉시 pop 제거
- `docker-compose.override.yml` — 로컬 오버라이드 (untracked)
- `.env` — REDIS_PASSWORD 설정 (커밋 대상 아님)

## 현재 상태 (Current State)

- 전체 22개 컨테이너 정상 (healthy)
- vLLM v0.16.0-cu130 정상 기동 (EXAONE + KURE)
- Airflow 22개 DAG 전부 success
- Emergency stop 활성 상태 (`trading_flags:stop = 1`)
- 포트폴리오: 5종목 (현대차, 고려아연, KODEX200, HD현대, 대한조선) — KIS/DB 일치
- GitHub Actions runner `ms-01` online

## 다음 할 일 (Next Steps)

- [ ] Emergency stop 해제 시점 결정 (시장 상황 판단 후)
- [ ] Monitor _pending_sell 동작 실전 검증 (장중 매도 시그널 → 체결 → refresh 사이클)
- [ ] stop_loss 기준 재검토 — 현재 4종목이 -10% 이상 손실 상태에서 반복 시그널 발생 중
- [ ] `docker-compose.override.yml` gitignore 추가 여부 검토

## Context for Next Session

다음 세션 시작 시 아래 파일들을 먼저 읽어주세요:
- `prime_jennie/services/monitor/app.py` — _pending_sell 로직 확인
- `prime_jennie/services/monitor/exit_rules.py` — 매도 규칙 (stop_loss 기준)
- `docker-compose.yml` — 서비스 구성 전체
- `.ai/RULES.md` — 세션 규칙

## 핵심 결정사항 (Key Decisions)

1. **CUDA compat 해결**: 드라이버 업그레이드 대신 LD_LIBRARY_PATH 순서 조정 선택
   - 이유: 드라이버 575(580.126.09)이 안정적이고, compat lib만 우회하면 됨
2. **Monitor pop 제거**: 시그널 발행 즉시 pop → pending_sell 플래그
   - 이유: 시그널 발행 ≠ 체결 확인. emergency stop 시 매도 안 되는데 포지션 사라지는 문제
3. **Redis 인증 추가**: UFW로 외부 차단했지만 방어 in depth
4. **vLLM v0.16.0-cu130**: v0.15.1에서 업그레이드. EXAONE 4.0 AWQ 정상 동작 확인

## 주의사항 (Warnings)

- **장중(09:00~15:30 KST) git push 금지** — 자동 배포 트리거됨
- **kis-gateway 장중 재시작 절대 금지** — KIS 토큰 캐시 소실 위험
- **`/var/run/cdi/nvidia.yaml`** — 재부팅 시 CDI가 다시 생성될 수 있으므로 LD_LIBRARY_PATH 유지 필요
- **emergency stop 상태**: `trading_flags:stop = 1` — 매수/매도 중단 중
- **Monitor 매도 시그널 반복**: 4종목이 stop_loss 기준 초과 → 5분마다 시그널 재발행 (pending_sell로 1회로 제한, refresh 시 리셋)
