# Session Handoff — 2026-03-03 폭락장 긴급 버그 수정

## 작업 요약 (What was done)

KOSPI -5.57% 폭락일(2026-03-03)에 발견된 3가지 버그 + 추가 발견 1건 수정 및 배포 완료.

### 커밋 목록 (development 브랜치)
1. `3ff061d` fix: 매도 체결 확인 강화 — 부분 체결 처리 + 취소 후 재확인
2. `50ebf42` feat: Council strategies_to_avoid → Scanner 전략 정합성 게이트 연결
3. `84ef4bf` fix: BULL regime 기준 55→60 (score 55-59 = SIDEWAYS)
4. `70a799e` fix: lint — 미사용 import 제거 및 정렬
5. `4efa4ff` fix: emergency stop Redis 키 불일치 수정 (trading:stopped → trading_flags:stop)

### Fix 1: 매도 체결 확인 False Negative 방지 (CRITICAL)
- **원인**: `confirm_order()` 3회×2초=6초 폴링 → 장전/동시호가 주문은 시간 부족
- **변경**:
  - `client.py` — 기본 폴링 5회×3초=15초, 부분 체결 감지 추가
  - `seller/executor.py` — FORCED_LIQUIDATION 10회×5초=50초 확장 폴링, 취소 후 재확인(recheck) 패턴
  - `buyer/executor.py` — 동일 취소-재확인 패턴 적용, `actual_qty` 사용
- **테스트**: test_sell_executor.py 3건, test_executor.py 2건 추가

### Fix 2: Council strategies_to_avoid → Scanner 연결
- **원인**: Council이 전략 회피 목록 출력하지만 Scanner까지 전달 안됨
- **변경**:
  - `macro.py` — TradingContext에 `strategies_to_avoid: list[str] = []` 필드 추가
  - `jobs/app.py` — `_update_trading_context()` 시그니처 확장, CouncilResult 전달
  - `risk_gates.py` — `check_strategy_alignment()` 함수 추가
  - `scanner/app.py` — `_publish_signal()`에 전략 정합성 체크 추가
- **테스트**: test_risk_gates.py TestStrategyAlignment 5건 추가

### Fix 3: Regime 매핑 BULL 기준 55→60
- **원인**: score 55가 BULL → Death Cross 비활성, 관대한 RSI 가드 유지
- **변경**: `jobs/app.py` `elif score >= 55:` → `elif score >= 60:`
- **테스트**: test_trading_context.py 신규 생성 (경계값 11케이스)

### Fix 4: Emergency Stop Redis 키 불일치 (현장 발견)
- **원인**: Telegram handler는 `trading_flags:stop` 설정, buyer/seller executor는 `trading:stopped` 확인 → `/stop` 명령 무효
- **변경**: 양쪽 executor의 `EMERGENCY_STOP_KEY`를 `"trading_flags:stop"`으로 통일
- **buyer executor**: `TRADING_PAUSED_KEY`도 `"trading_flags:pause"`로 통일

## 현재 상태 (Current State)

- **배포 완료**: 5개 커밋 모두 push + GHCR 빌드 + self-hosted 배포 완료
- **테스트**: 740건 전체 통과
- **Emergency Stop 활성**: `trading_flags:stop=1` (Redis) — 매수/매도 모두 차단 중
- **Council 결과**: sentiment=neutral_to_bearish, score=30, regime=BEAR
- **TradingContext**: BEAR regime, strategies_to_avoid=["MOMENTUM", "MOMENTUM_CONTINUATION", "VOLUME_BREAKOUT"]
- **Scout 완료**: BEAR 국면 기반 watchlist 갱신됨 (25종목)
- **알려진 이슈 없음**: 긴급 수정 사항 모두 반영 확인

## 다음 할 일 (Next Steps)

- [ ] **장 시작 전 /stop 해제 여부 결정** — 시장 상황 판단 후 `trading_flags:stop` 제거
- [ ] **폭락장 사후분석**: trade_log + positions DB로 실제 피해 규모 확인
  - 미체결로 기록됐지만 실제 체결된 매도 건 찾아서 수동 보정 필요할 수 있음
  - 특히 Fix 1 이전에 실행된 매도 주문들 확인
- [ ] **MEMORY.md 업데이트**: regime 매핑 기준 55→60 변경 반영 (현재 MEMORY에 `>= 55 BULL` 기록됨)
- [ ] **main 브랜치 머지 검토**: development에 충분히 안정화되면 main PR

## Context for Next Session

다음 세션 시작 시 아래 파일들을 먼저 읽어주세요:
- `prime_jennie/services/seller/executor.py` — 매도 체결 확인 재확인 로직 (L219-255)
- `prime_jennie/services/buyer/executor.py` — 매수 체결 확인 재확인 로직 (L276-310)
- `prime_jennie/services/scanner/risk_gates.py` — 전략 정합성 게이트 (check_strategy_alignment)
- `prime_jennie/services/jobs/app.py` L1369-1410 — regime 매핑 + strategies_to_avoid 전달 로직

## 핵심 결정사항 (Key Decisions)

1. **취소-재확인 패턴 채택**: 미체결 판정 → cancel_order() → check_order_status() 1회 추가. KIS API 특성상 체결 반영이 느릴 수 있어, 취소 시도 후 재확인으로 false negative 방지
2. **FORCED_LIQUIDATION 확장 폴링**: 50초(10×5s)로 일반 매도(15초) 대비 3배 이상. 강제 청산은 반드시 체결 확인 필요
3. **BULL 기준 60으로 상향**: score 55-59 구간이 폭락장에서도 BULL 유지되는 문제 → SIDEWAYS로 변경. Death Cross 활성화, 타이트닝 10일, 보수적 포지션 적용
4. **Redis 키 네이밍 통일**: `trading_flags:` prefix가 정본 (Telegram handler 기준). executor 쪽이 잘못된 레거시 키 사용 중이었음

## 주의사항 (Warnings)

- **MEMORY.md regime 매핑 미갱신**: `>= 55 BULL` → `>= 60 BULL` 변경이 MEMORY.md에 아직 미반영
- **미체결-실체결 불일치 데이터**: Fix 1 이전에 "error"로 기록된 매도 건 중 실제 체결된 것이 있을 수 있음. DB 수동 점검 필요
- **장중 배포 금지 원칙**: 오늘은 장 마감 후(15:30 이후) 긴급 배포 진행. 평시에는 반드시 15:30 이후 push
- **Emergency stop 활성 상태**: `trading_flags:stop=1` — 다음 거래일 시작 전 해제 여부 사용자 판단 필요
