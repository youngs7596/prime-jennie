# Session Handoff — 2026-03-05-23:15

## 작업 요약 (What was done)

News pipeline 진단 결과 발견된 3건의 치명적 버그를 수정하고 배포 완료.

### 1. Analyzer `model=None` 버그 수정 (Fix 2 — 가장 치명적)
- **문제**: `analyzer.py:198`에서 `generate_json(model=None)` 호출 → `BaseLLMProvider.generate_json()`에 `model` 파라미터 없음 → **매번 TypeError** → except 절에서 무조건 fallback `{score: 50}` 반환 → EXAONE이 한 번도 뉴스 분석에 사용되지 않았음
- **증거**: DB 조회 결과 최근 7일 **20,147건 전부 score=50**, reason="분석 불가 — 기본 중립"
- **수정**: `model=None` 파라미터 제거, exception 로그 debug→warning(exc_info=True), dedup skip 로깅 추가
- **파일**: `prime_jennie/services/news/analyzer.py`

### 2. vLLM EXAONE 토큰 초과 400 에러 수정 (Fix 1)
- **문제**: `ollama.py` — `len(prompt) // 2`로 한국어 토큰 추정 → 실제보다 과소평가, safety margin 64 부족, system prompt 길이 미반영
- **수정**:
  - `_clamp_max_tokens()`: `len(prompt) // 2` → `len(prompt)` (보수적 1:1 추정), safety margin 64 → 128
  - `generate()`: system prompt도 clamping에 반영 (`full_prompt = (system or "") + prompt`)
  - `reporter.py`: `max_tokens=3000` → `2000`
  - `analyst.py`: `max_tokens=4096` → `2048`
- **파일**: `prime_jennie/infra/llm/providers/ollama.py`, `prime_jennie/services/briefing/reporter.py`, `prime_jennie/services/scout/analyst.py`

### 3. 우선주 코드 필터링 (Fix 3)
- **문제**: `stock_masters` DB에 `00806K`, `33626L` 등 우선주 코드 존재 → `StockCode(^\d{6}$)` 검증 실패 → 크롤링마다 수십 건 warning + 불필요한 HTTP 요청
- **수정**: `_load_universe()`에 `len(s.stock_code) == 6 and s.stock_code.isdigit()` 필터 추가
- **파일**: `prime_jennie/services/news/app.py`

### 테스트 추가
- `test_llm_providers.py`: `test_clamp_includes_system_prompt`, `test_clamp_returns_min_256`
- `test_news_pipeline.py`: `test_analyze_sentiment_calls_generate_json` (model= 파라미터 부재 검증), `test_analyze_sentiment_fallback_on_error`, `test_load_universe_filters_preferred_stocks`

### 커밋 이력
- `470512f` — fix: news pipeline 3건 버그 수정
- `db9db3d` — style: ruff format 적용
- `64b29ee` — fix: test에서 redis.ResponseError 사용 (BUSYGROUP mock)

## 현재 상태 (Current State)
- **branch**: `development` = `64b29ee`
- **CI**: 683 tests passed, lint/format 클린
- **배포**: GHCR Publish + Deploy 완료 (workflow_dispatch `22673168959`)
- **news-pipeline 컨테이너**: 새 이미지로 재시작 완료
- 현재 장외 시간이라 다음 수집 사이클(30분 간격)부터 EXAONE 실제 분석 시작

## 다음 할 일 (Next Steps)
- [ ] **감성 분석 실제 동작 확인**: 다음 수집 사이클 후 DB에서 `sentiment_score != 50`인 레코드 확인
  ```sql
  SELECT COUNT(*), AVG(sentiment_score), MIN(sentiment_score), MAX(sentiment_score)
  FROM stock_news_sentiments
  WHERE news_date >= CURDATE() AND sentiment_score != 50;
  ```
- [ ] **news-pipeline 로그 모니터링**: EXAONE POST 호출 발생 확인, token overflow 400 에러 없는지 확인
  ```bash
  docker logs prime-jennie-news-pipeline-1 --since 30m | grep -E "Sentiment|EXAONE|400"
  ```
- [ ] **우선주 warning 사라짐 확인**: 크롤링 로그에서 `00806K`, `33626L` 등 warning 미발생 확인
- [ ] **기존 score=50 데이터 처리 방침 결정**: 20,147건 전부 잘못된 데이터 — 삭제 or 재분석 or 유지 결정 필요

## 핵심 결정사항 (Key Decisions)
- 토큰 추정: 한국어 1:1 보수적 비율 (over-estimate 방향, 안전)
- max_tokens 축소: reporter 2000, analyst 2048 (EXAONE max_model_len=4096 기준 충분)
- 우선주 필터: `isdigit()` + `len==6`으로 단순 필터 (K/L/G suffix 패턴 개별 체크 불필요)

## 주의사항 (Warnings)
- `LLM_VLLM_MAX_MODEL_LEN=4096` — EXAONE 모델 설정과 반드시 일치해야 함
- Fix 2 수정으로 실제 LLM 호출이 발생하면 vLLM 서버 부하 증가 예상 — GPU 모니터링 필요
- 장중(09:00~15:30 KST) git push 금지
