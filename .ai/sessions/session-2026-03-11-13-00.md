# Session Handoff - 2026-03-11-13:00

## 작업 요약 (What was done)

### GAP_UP_REBOUND 전략 v1 구현 + 배포
- **RSI_REBOUND 전략 코드 완전 삭제**: `detect_rsi_rebound()`, `_check_rsi_rebound()` 함수 삭제
- **GAP_UP_REBOUND 전략 신규 구현**:
  - Scanner: `detect_gap_up_rebound()` — 전일대비 +2% 갭업, 거래량 1.5x, 시가 유지 조건
  - Scanner: Gateway API로 전일종가/시가 캐시 (`_daily_ref` dict, `load_daily_refs()`)
  - Scanner: ORB_BREAKOUT와 동일한 partial risk gate bypass 패턴 적용
  - Backtest: `_check_gap_up_rebound()` — 전일 -3%+ 하락 후 갭업 조건 추가
  - 테스트 5건 추가 (TestGapUpRebound)
- **Shadow Comparison 삭제**: `scout/quant.py` `_log_shadow_comparison()` 100줄 삭제 (TODO #5)
- **SignalType.GAP_UP_REBOUND** enum 추가, RSI_REBOUND deprecated 표기

### 배포
- 첫 push: CI 실패 (`scripts/sweep_rsi_threshold.py` ruff SIM102 에러)
- 수정 후 재push: CI pass → GHCR 빌드 → deploy 성공
- 서버 확인: 22개 서비스 재시작, `load_daily_refs: 24 stocks` 정상 로드

### 변경된 파일
- `prime_jennie/domain/enums.py` — GAP_UP_REBOUND enum 추가
- `prime_jennie/services/scanner/strategies.py` — detect_gap_up_rebound() 추가, detect_rsi_rebound() 삭제
- `prime_jennie/services/scanner/app.py` — daily_ref 캐시, GAP_UP_REBOUND 감지 블록
- `prime_jennie/services/backtest/daily_strategies.py` — _check_gap_up_rebound() 추가
- `prime_jennie/services/scout/quant.py` — _log_shadow_comparison() 삭제
- `tests/unit/services/test_strategies.py` — TestGapUpRebound 5건
- `scripts/sweep_rsi_threshold.py` — ruff SIM102 수정
- `.ai/TODO.md`, `.ai/DONE.md` — 과제 업데이트

## 현재 상태 (Current State)
- **배포 완료**: GAP_UP_REBOUND 전략이 운영 환경에 배포됨
- **stop=1 유지**: 시그널 감지 + signal_logs 기록만, 실제 매수 없음
- **데이터 수집 시작**: 내일(03-12) 장 시작부터 GAP_UP_REBOUND 시그널 데이터 축적
- **regime**: SIDEWAYS, watchlist 25종목

## 다음 할 일 (Next Steps)
- 이 세션에서 새로 발견된 이슈 없음
- 기존 과제 추적: `.ai/TODO.md` 참조

## Context for Next Session
다음 세션 시작 시 아래 파일들을 먼저 읽어주세요:
- `.ai/TODO.md` — 미해결 과제 현황
- `.ai/DONE.md` — 최근 완료 항목 확인
- `prime_jennie/services/scanner/app.py` — GAP_UP_REBOUND 동작 확인 시

## 핵심 결정사항 (Key Decisions)
- **GAP_UP_REBOUND을 stop=1 상태로 먼저 배포**: 시그널 데이터를 30~45 거래일 축적 후 성과 평가 → stop 해제 결정
- **partial risk gate bypass 패턴 채택**: rsi_guard, micro_timing, market_regime 스킵 (ORB_BREAKOUT과 동일)
- **전일종가 계산 방식**: Gateway snapshot의 `price / (1 + change_pct / 100)` 역산

## 주의사항 (Warnings)
- `load_daily_refs()`는 장 시작 시 + watchlist 리로드 시 호출됨. Gateway가 아직 기동 전이면 502 발생 가능 (graceful 처리됨)
- RSI_REBOUND enum은 DB 호환용으로 유지 (deprecated 표기)
- `sweep_rsi_threshold.py`는 RSI_REBOUND 분석용 보존 스크립트 (운영 미사용)
