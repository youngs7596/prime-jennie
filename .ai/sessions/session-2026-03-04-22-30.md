# Session Handoff - 2026-03-04-22-30

## 작업 요약 (What was done)

### 1. 개발 테스트베드 구축 (완료)
- `jennie_db_dev` DB 생성 + 14개 참조 테이블 VIEW 교체
- Redis DB 1번 분리, `.env.dev` 생성
- `scripts/setup_dev.sh` 원샷 자동화 스크립트
- `scripts/setup_dev_views.sql` VIEW 생성 SQL
- FK constraint, alembic 004 fresh-DB 호환 이슈 해결

### 2. 6개 운영 태스크 (전부 완료)
1. **VIX 23.57 조사**: 정상 — 실제 3/3 US 종가, API 정상 동작
2. **고아 Redis 키 삭제**: `market:regime:data` + `llm:stats` 4건
3. **Dashboard/Backtest regime 매핑 통일**: sentiment 문자열 → score 기반으로 통일
4. **Circuit Breaker 구현**: KOSPI -2%/-4%, VIX≥35 시 regime downgrade (8개 테스트)
5. **긴급정지 해제 분석**: 5개 포지션 중 3개 손절선 이탈, 시장 안정화 대기 권고
6. **Monitor _pending_sell 검증**: 정상 동작 확인

### 3. WSJ Newsletter → Macro Council 파이프라인 (완료)
- `prime_jennie/infra/crawlers/wsj_gmail.py` — Gmail API OAuth2 기반 수집
- WSJ 4종 (The 10-Point, Markets A.M./P.M., What's News) 자동 수집
- `council_trigger()`에 WSJ 브리핑 통합, 실패 시 graceful skip
- Google OAuth2 초기 인증 완료, `token.json` 생성됨
- MS-01로 `credentials.json` + `token.json` 복사 완료

## 현재 상태 (Current State)
- **development 브랜치**: 모든 변경사항 커밋 완료, main에 머지/push 완료
- **MS-01**: 배포 자동 트리거됨 (GHCR → pull+up)
- **675개 테스트 전부 통과**, lint 클린
- `trading_flags:stop=1` 유지 중 (긴급정지 해제 전)

## 다음 할 일 (Next Steps)
- [ ] **긴급정지 해제 타이밍 판단**: 시장 안정화 확인 후 stop 해제
- [ ] **WSJ Markets P.M. / What's News 실제 수신 확인**: 구독 직후라 아직 몇 건만 도착, 며칠 후 4종 모두 정상 수신 확인 필요
- [ ] **Council 결과에 WSJ 반영 효과 확인**: council-trigger 로그에서 WSJ briefing 길이 확인
- [ ] **token.json 자동 갱신 모니터링**: OAuth refresh token 만료 시 재인증 필요 (보통 6개월)
- [ ] **dev 환경 서비스 로컬 실행 테스트**: `.env.dev`로 scanner 등 기동 확인

## Context for Next Session
다음 세션 시작 시 아래 파일들을 먼저 읽어주세요:
- `prime_jennie/infra/crawlers/wsj_gmail.py` — WSJ Gmail 크롤러
- `prime_jennie/services/jobs/app.py:1312-1330` — council trigger의 WSJ 통합 부분
- `prime_jennie/services/council/pipeline.py` — CouncilInput/CouncilPipeline

## 핵심 결정사항 (Key Decisions)
- **Gmail API OAuth2 선택** (IMAP 대신): credentials.json + token.json 파일 기반, App Password 불필요
- **Just-in-time 수집**: 별도 DAG/사전 적재 없이 council 트리거 시점에 Gmail API 1회 호출로 취합
- **newer_than:2d + 타입별 최신 1건**: 도착 시간이 다른 4종을 자연스럽게 취합
- **3000자 truncation**: council 프롬프트 과부하 방지
- **Circuit breaker는 downgrade only**: macro_quick 이후 실행, 절대 upgrade 안 함

## 주의사항 (Warnings)
- `credentials.json`, `token.json`은 `.gitignore`에 등록됨 — 서버에 수동 복사 필요
- `token.json` OAuth refresh token은 약 6개월 후 만료 — 재인증 필요
- 장중(09:00~15:30 KST) git push 금지
- `trading_flags:stop=1` 해제 전 시장 상황 반드시 확인
