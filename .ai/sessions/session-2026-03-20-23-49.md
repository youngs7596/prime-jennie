# Session Handoff - 2026-03-20-23:49

## 작업 요약 (What was done)

### 1. 시스템 전체 점검 (MS-01 서버)
- 22개 컨테이너 전체 healthy (7일+ uptime) 확인
- trading_flags: stop=1, pause=emergency_stop 유지 중
- trading_context: SIDEWAYS (score=58, 03-20 02:53 업데이트)
- Council: 정상 실행 (03-15~20 매일 2회 완료)
- KIS Gateway / News Pipeline / Executors: 에러 없음
- Scout: DeepSeek 일시 장애 → OpenRouter 자동 failover 정상

### 2. Gmail OAuth 토큰 수정 (CRITICAL)
- **문제**: refresh token revoked → WSJ 수집 03-15~20 (5일간) 실패
- **수정 1**: `wsj_gmail.py` — `creds.refresh()` 실패 시 `InstalledAppFlow` 재인증으로 fallback
  (기존: 예외가 그대로 올라가 수집 중단)
- **수정 2**: WSL2에서 OAuth 재인증 → 새 token.json 발급 → `scp`로 서버 복사
- **검증**: WSJ 3건 정상 수집 확인 (markets-am, 10-point, markets-pm)

### 3. Docker Zombie Process 수정
- **문제**: 226개 zombie curl 프로세스 (Dockerfile HEALTHCHECK의 curl이 PID 1 uvicorn에 의해 미수거)
- **수정**: `docker-compose.yml` x-service-defaults에 `init: true` 추가 (tini가 PID 1로 zombie 자동 수거)
- 다음 컨테이너 재시작 시 적용됨

### 4. 종목 0126Z0 (삼성에피스홀딩스) 비활성화
- KIS API가 422 Unprocessable Entity 반환 (300건 중 1건)
- DB에서 `is_active = 0` 처리 완료

### 5. 시그널 기반 가상 포트폴리오 시뮬레이션
- 2.1억 기준, 03-06~20 BUY 시그널 기반 백테스트 수행
- RSI_REBOUND 포함: +0.52% (10종목, 승률 50%)
- RSI_REBOUND 제외: -0.15% (10종목, 승률 60%)
- 핵심 인사이트: 전략 종류보다 종목 선택·진입 시점이 더 결정적

### 변경된 파일
- `prime_jennie/infra/crawlers/wsj_gmail.py` — OAuth refresh fallback 추가
- `docker-compose.yml` — `init: true` 추가 (zombie reap)

## 현재 상태 (Current State)

- **시스템**: 22개 컨테이너 전체 healthy
- **Trading flags**: stop=1 (억제 모드)
- **Regime**: SIDEWAYS (score=58)
- **Gmail**: 토큰 갱신 완료, 다음 Council에서 WSJ 수집 정상화 예정
- **서버**: 226 zombie 잔존 (다음 재시작 시 해소), System restart required 경고, 14 updates 대기

## 다음 할 일 (Next Steps)

- 서버 컨테이너 재시작하여 `init: true` + zombie 해소 적용 (장외 시간에)
- 서버 OS 업데이트 14건 + 보안 업데이트 1건 적용 검토 (System restart required)
- TODO #24 GAP_UP_REBOUND 시그널 데이터 축적 경과 확인 (30~45 거래일 목표)

## Context for Next Session
- `.ai/TODO.md` — 미해결 과제 현황
- `.ai/OPS.md` — 서버 운영 명령어 참조

## 핵심 결정사항 (Key Decisions)

- Gmail OAuth fallback: refresh 실패 시 자동 재인증 플로우로 전환 (수동 개입 최소화)
- Docker init: true로 zombie 방지 (모든 앱 컨테이너 공통 적용)

## 주의사항 (Warnings)

- `trading_flags:stop=1` 유지 중
- 서버 zombie 226개는 현재 무해하나, 컨테이너 재시작 전까지 잔존
- 서버 `System restart required` — 커널 업데이트 필요, 재부팅 시 전체 서비스 다운타임 발생
